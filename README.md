# Simple REST API with Express & MongoDB

### How to use
Clone and run `npm init`.

### How to run
Just run `node server.js` in terminal

### Endpoint
```
GET /api
POST /api/users
PUT /api/update-user/:id
DELETE /api/delete-user/:id
```

Enjoy it....
